/** Supabase REST / mirror — tolerate slow VPN without hanging the UI. */
export const CLOUD_REQUEST_MS = 8_000;

/** Cold-start auth session read. */
export const SUPABASE_AUTH_MS = 8_000;

/** Service worker HTML navigation before cache / shell fallback. */
export const SW_NAVIGATION_MS = 5_000;
